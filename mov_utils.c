#include "so_long.h"
#include "so_long_utils.h"

void	key(int key, t_game *game)
{
	if (key == ESC)
		end_game(&game);
	if (key == )
		down(&game);
	if (key == )
		up(&game);
	if (key == )
		left(&game);
	if (key == )
		right(&game);
}